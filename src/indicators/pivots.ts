/**
 * @param candles
 * @param leftBars
 * @param rightBars
 */
export function pivotHighs(
  candles: CandleData[],
  leftBars: number,
  rightBars: number
) {
  return;
}

/**
 * @param candles
 * @param leftBars
 * @param rightBars
 */
export function pivotLows(
  candles: CandleData[],
  leftBars: number,
  rightBars: number
) {
  return;
}
